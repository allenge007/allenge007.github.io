{
  "name": "allenge-personal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "ASTRO_TELEMETRY_DISABLED=1 astro dev",
    "prepare:notes": "node scripts/prepare-notes.mjs",
    "check": "node scripts/prepare-notes.mjs && ASTRO_TELEMETRY_DISABLED=1 astro check",
    "build:astro": "node scripts/prepare-notes.mjs && ASTRO_TELEMETRY_DISABLED=1 astro build",
    "build:search": "pagefind --site dist",
    "build": "node scripts/prepare-notes.mjs && ASTRO_TELEMETRY_DISABLED=1 astro build && node scripts/postbuild.mjs && pagefind --site dist",
    "test:links": "node scripts/check-links.mjs",
    "test:notes": "node scripts/check-notes-regression.mjs",
    "test:content": "node scripts/check-content-contracts.mjs",
    "verify": "node scripts/prepare-notes.mjs && ASTRO_TELEMETRY_DISABLED=1 astro check && ASTRO_TELEMETRY_DISABLED=1 astro build && node scripts/postbuild.mjs && pagefind --site dist && node scripts/check-links.mjs && node scripts/check-notes-regression.mjs && node scripts/check-content-contracts.mjs"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@astrojs/mdx": "^7.0.3",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource/solway": "^5.2.8",
    "astro": "^7.0.9",
    "katex": "^0.17.0",
    "mermaid": "^11.16.0",
    "rehype-katex": "^7.0.1",
    "remark-gfm": "^4.0.1",
    "remark-math": "^6.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "pagefind": "^1.5.2",
    "typescript": "^6.0.3"
  }
}
