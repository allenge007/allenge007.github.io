# lec8:

## slater 条件

!!! note
    $relint$：相对内部

    $aff$：仿射包

    $$
    {\rm relint\ }D = \{x\in D | B(x, r)\cap {\rm aff\ }D \subseteq D, \exist r > 0 \} \\
    \exist x \in {\rm relint\ }D, f_i(x) < 0, h_j(x) = 0
    $$