name: Build and deploy the combined site

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      # npm 11.16.0 (bundled with the current Node 24 runner image) rejects
      # this otherwise valid lockfile with a bare `Invalid Version:` error.
      # Keep CI on the npm release used to generate and verify the lockfile.
      - name: Use the verified npm release
        run: npm install --global npm@11.6.2

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Install MkDocs dependencies
        run: pip install -r requirements.txt

      - name: Type-check Astro
        run: npm run check

      - name: Build Astro, Notes, CNAME, and Pagefind
        run: npm run build

      - name: Verify links and content contracts
        run: npm run test:links && npm run test:content

      - name: Upload combined build
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: combined-site
          path: dist
          if-no-files-found: error

      - name: Deploy dist to gh-pages root
        if: github.event_name != 'pull_request'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          cname: www.allenge.me
          force_orphan: true
